else {
			movePile(n-1,from,to,via);
			System.out.println("Ring moved from " + from + " to " + to);       
			movePile(n - 1, via, from, to);
		    
		}
    
    // A part of movePile()
    public void testSizeEmpty() {
		// test l.size() for an empty linked list
		assertNotEquals("Is not an empty linked list", 0,l.size());
	}
// Have some error
throw new RuntimeException("Is empty!");


# for
list_a = list()
for a in range(5):
    list_a.append(a)
print(list_a)
# 
list_b = [b for b in range(5)]
